version: '3.8'

services:
  # API service
  api:
    build:
      context: .
      target: api
    ports:
      - "8000:8000"
    volumes:
      - "./best_model.pth:/app/best_model.pth"  # Mount model file
      - "./data:/app/data"  # Mount data directory
    environment:
      - MODEL_PATH=best_model.pth
    restart: unless-stopped

  # Demo UI service
  demo:
    build:
      context: .
      target: demo
    ports:
      - "8501:8501"
    volumes:
      - "./best_model.pth:/app/best_model.pth"  # Mount model file
    restart: unless-stopped

  # Training service (run once)
  train:
    build:
      context: .
      target: train
    volumes:
      - "./data:/app/data"  # Mount training data
      - ".:/app/outputs"    # Mount current directory for output
    working_dir: /app
    command: ["python", "src/train.py"]
    profiles: ["training"]  # Only run when explicitly requested
